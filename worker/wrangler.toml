name = "escadmin-handoff-push"
main = "src/index.js"
compatibility_date = "2026-01-01"

# 1분마다 새 인수인계가 있는지 확인한다
[triggers]
crons = ["* * * * *"]

# 아래 둘은 앱 번들에도 이미 공개돼 있는 값이라 비밀이 아니다.
# 진짜 비밀인 VAPID_PRIVATE_KEY 는 `wrangler secret put` 으로 넣는다.
[vars]
FIREBASE_PROJECT_ID = "escadmin-c7655"
FIREBASE_API_KEY = "AIzaSyBYkUHgM9Xk9cwRrinOCJ7RvcYGyJeYapE"
VAPID_SUBJECT = "mailto:namho1123@gmail.com"
# 앱(src/data/pushConfig.js)에 들어간 것과 같은 값이어야 한다. 공개 열쇠라 비밀이 아니다.
VAPID_PUBLIC_KEY = "BHrxN1L-t5kyrG7VhCGd5W8iSE7UouW9en6SfigQffM-UsINz-pEw5IGH9cb_bKE7yAkaGALChKxOS3nNrJRqMw"
